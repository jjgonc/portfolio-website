# portfolio-website

### To run in localhost:

`cd portfolio`

`npm start` to start in localhost

----
### To deploy:

`git add*`; `git commit -m "new"`; `git push`

`cd ..`

`npm run build`

`npm run deploy` to deploy
